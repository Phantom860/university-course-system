server:
  address: 0.0.0.0
  port: 8080
  servlet:
    context-path: /api

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sjkdzy?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: 3271149877mm
    driver-class-name: com.mysql.cj.jdbc.Driver

  # Session配置
  session:
    timeout: 1800  # 30分钟，单位秒
    store-type: none  # 使用默认的Tomcat Session

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.university.coursesystem.entity
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

logging:
  level:
    com.university.university_course_system: DEBUG
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %logger{36} - %msg%n"
